{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard
{% endblock %}

{% block main %}
	<h1 class="text-danger mb-4">Dashboard Admin</h1>

	<div class="row">
		<div class="col-md-4 mb-3">
			<a href="{{ path('admin_manage_car_articles') }}" class="btn btn-danger w-100">Gérer Articles</a>
		</div>
		<div class="col-md-4 mb-3">
			<a href="{{ path('admin_manage_store') }}" class="btn btn-danger w-100">Gérer Store</a>
		</div>
		<div class="col-md-4 mb-3">
			<a href="{{ path('admin_manage_sport_auto') }}" class="btn btn-danger w-100">Gérer Sport Auto</a>
		</div>
	</div>
{% endblock %}
