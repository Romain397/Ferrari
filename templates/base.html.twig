<!DOCTYPE html>
<html lang="fr-FR">
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Ferrari WEC
			{% endblock %}
		</title>

		{# Lien vers le fichier CSS #}
		<link rel="stylesheet" href="{{ asset('css/style.css') }}">
	</head>

	<body>

		<header>
			<h1>Ferrari – WEC 🏁</h1>
			<nav>
				<a href="{{ path('home') }}">Accueil</a>
				<a href="{{ path('ferrari') }}">Ferrari</a>
				<a href="{{ path('random') }}">Citation</a>
			</nav>
		</header>

		<main>
			{% block body %}{% endblock %}
		</main>

	</body>
</html>
